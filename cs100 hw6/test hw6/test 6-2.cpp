#include<iostream>
#include<string>
#include<vector>
#include <sstream>
using namespace std;
int main()
{
	//int alarm_num;
	//cin >> alarm_num;
	//vector<string>vector_a(alarm_num);
	//vector<string>vector_b(alarm_num);
	//vector<string>vector_c(alarm_num);
	//for (int i = 0; i < alarm_num; i++) {
	//	string a, b, c;
	//	cin >> a >> b >> c;
	//	vector_a[i] = a;
	//	vector_b[i] = b;
	//	vector_c[i] = c;
	//}
	//for (int i = 0; i < alarm_num; i++) {
	//	cout << vector_a[i] << endl;
	//	cout << vector_b[i] << endl;
	//	cout << vector_c[i] << endl;
	//}
	//cout << vector_b[0][0]-'0' << endl;
	//cout << vector_b[0][1] << endl;
	//cout << vector_b[0][3] << endl;
	//cout << vector_b[0][4] << endl;

	////以下是记录总共的minute
	//int minute=0;
	//minute = ((vector_b[0][0] - '0') * 10 + (vector_b[0][1] - '0')) * 60 + (vector_b[0][3] - '0') * 10 + (vector_b[0][4] - '0');
	//cout << minute << endl;


	/*vector<int> m_alarms;
	m_alarms.push_back(2);
	m_alarms.push_back(3);
	cout << m_alarms[0] << endl;
	cout << m_alarms[1] << endl;*/

	int a = 3;
	string b;
	b = "3";
	cout << sizeof(b) << endl;
	
}